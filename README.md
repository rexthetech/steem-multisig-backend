# steem-multisig-backend

## Install

```
npm install
```

Requires MySQL DB (schema in db-schema.sql).

Edit config.js for DB details, and steem-multisig instance's URL (for CORS)

## Run

```
node server.js
```
